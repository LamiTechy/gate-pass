# Example environment variables for production
# Copy this to .env.local and fill in actual values

# Frontend (for Vite)
VITE_API_URL=https://your-backend-domain.com

# Backend (Node.js) - only needed if backend is deployed separately
PORT=4001
NODE_ENV=production
